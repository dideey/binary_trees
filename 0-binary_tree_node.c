#include "binary_trees.h"

/**
 * allocate space for node
 * 
*/

binary_tree_t *binary_tree_node(binary_tree_t *parent, int value)
{
    newnode = malloc(size of(parent));
    newnode-> item = value
    newnode-> left = NULL
    newnode-> right = NULL
}
